# Copy this file to .env and update values for your environment

# Environment: local | staging | production
NODE_ENV=staging

# ============================================
# URL Configuration (Choose ONE approach)
# ============================================

# Option 1: Full URLs (for staging/production)
API_BASE_URL=https://bgshop.work.gd
FRONTEND_BASE_URL=https://team-challange-front.vercel.app

# Option 2: Host + Port (for local/Docker development)
# Uncomment and comment out Option 1 if using Docker
# API_HOST=localhost
# API_PORT=3000
# API_PROTOCOL=http
# FRONTEND_HOST=localhost
# FRONTEND_PORT=3001
# FRONTEND_PROTOCOL=http

# Timeouts (in milliseconds)
API_TIMEOUT=30000
FRONTEND_TIMEOUT=30000

# Browser Settings
HEADLESS=false
SLOW_MO=0
BROWSER=chromium

# Test User (use test environment credentials)
TEST_USER_EMAIL=your-test-user@example.com
TEST_USER_PASSWORD=YourPassword123!
